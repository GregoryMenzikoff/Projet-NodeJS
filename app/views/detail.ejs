<%- include('partials/head') %>
  <main class="content">
    <section class="segment">
      <% for(const web of website) { %>
      <article class="detail">
        <header class="detail-header">
          <div class="detail-intro">
            <h1 class="detail-title">
              <%= web.title %>
            </h1>
            <h2 class="detail-subtitle">Je ne peux pas prendre RDV !</h2>
          </div>
          <button class="detail-share"><i data-feather="share-2"></i></button>
          <span class="detail-counter"><i data-feather="circle"></i> 180</span>
        </header>
        <figure class="detail-figure">
          <img src="../img/ph-screen.jpg" alt="">
          <figcaption class="detail-caption">Disfonctionnement de Doctolib</figcaption>
        </figure>
        <ul class="detail-list">
          <li><span class="detail-label">Reporteur</span> : Marie Dupont</li>
          <li><span class="detail-label">Type</span> : <%= web.level %></li>
          <li><span class="detail-label">Support</span> : <%= web.appareil %></li>
        </ul>
        <p class="detail-content"><%= web.description %></p>
        <footer class="detail-actions">
          <a class="btn">Voir le disfonctionnement</a>
          <button class="btn"><i data-feather="check"></i> Inaccessibilité corrigée</button>
          <button class="btn btn--red"><i data-feather="circle"></i> Lancer une tomate</button>
        </footer>
      </article>
      <% } %>
      <footer class="segment-actions">
        <a href="#" class="btn">
          Retour à la liste
        </a>
      </footer>
    </section>
  </main>
<%- include('partials/footer') %>

<!-- TODO etape 11 : reste à faire apparaitre dans le nouvel article les types et support dans le detail -->